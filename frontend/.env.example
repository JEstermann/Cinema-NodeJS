# Développement (`npm run dev`) : API locale.
VITE_API_URL=http://localhost:3000

# Prod : défaut dans client.ts = https://api.aimassi.dev (voir README). Surcharge ici ou au build Docker si autre domaine.
